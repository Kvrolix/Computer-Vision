clear;
% Read the image
img = imread('002.jpg');
imshow(img)


%convert to GrayScale
gray_img = rgb2gray(img);

% Compute a threshold level
threshold_level = 0.2;

%Convert the grayscale image ot binary by thresholding

bw_img = im2bw(gray_img,threshold_level);


imshow(bw_img)